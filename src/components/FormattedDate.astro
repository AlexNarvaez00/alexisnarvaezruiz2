---
interface Props {
  date: Date;
}

const { date } = Astro.props;
---

<time datetime={date.toISOString()} class="text-gray-600 dark:text-gray-200/50">
  {
    date.toLocaleDateString("ex-mx", {
      year: "numeric",
      month: "short",
      day: "numeric",
    })
  }
</time>
